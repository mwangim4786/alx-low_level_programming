#include "main.h"

/**
 * _abs - Get absolute value of an integer.
 * @n: The integer to be checcked.
 *
 * Return: The absolute value of the integer.
 */
int _abs(int n)
{
	if (n >= 0)
		return (n);
	else
		return (-n);
}
